## What's new

- **Version display fix** — release builds now show `vX.Y.Z` (with the `v` prefix) consistent with git tags, so the update check correctly reports "up to date" rather than falsely showing a new version as available.
